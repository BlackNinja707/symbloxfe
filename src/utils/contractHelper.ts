// import { ethers } from "ethers";
// import { readContract } from "@wagmi/core";
// import SymbloxABI from "../config/abis/ISymbloxToken.json";

// export const getSymbloxContract = (
//   address: string,
//   signer?: ethers.Signer | ethers.Provider
// ) => {
//   if (!address) return null;
//   return readContract(address, SymbloxABI);
// };

// export const getStakingContract = (
//   chainId?: number,
//   signer?: ethers.Signer | ethers.Provider,
//   version: string = "v2"
// ) => {
//   if (version === "v2") {
//     return readContract();
//   }
// };
